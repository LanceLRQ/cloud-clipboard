const antdConfig = {
    theme: {
        components: {
            Layout: {
                headerBg: '#fff',
                bodyBg: '#fff',
            }
        }
    }
}
export default antdConfig;